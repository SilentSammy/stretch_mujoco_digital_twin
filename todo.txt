- Can we use NormVelCtrl with regular Robot position commands?
Address the issue where algorithmic velocity commands for the base are not capped at 0.33, but TeleopCommands are, making it so that the operator may not fully be able to override base commands.
Non-blocking Robot.wait_command, call it is_command_finished or something like that. But where should this new function go?
